node_modules/

# PNG generados — se regeneran desde el spec
placas/

# Datos de clientes: cuentas, marcas, consumo y piezas generadas.
# Nunca se versionan.
data/

# Claves. Nunca se versionan — la plantilla es .env.ejemplo
.env

# Marcas o specs locales que no querés versionar
*.local.json

# Capturas de las pruebas de navegador
.playwright-mcp/
capturas/
web/capturas/

.DS_Store
.wrangler/
.vercel
.env*.local

# El comercial renderizado y sus fuentes pesadas: se regeneran con
# `node video/render.mjs` y no tienen por qué viajar en el repositorio.
video/comercial.mp4
video/assets/
Recursos video/
